import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Header } from '@/components/layout/Header'
import { Footer } from '@/components/layout/Footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Workeraa - Premier Recruitment & Staffing Solutions',
  description: 'Connect with top talent across IT, Finance, Sales & Marketing. Executive recruitment, permanent hiring, and flexible staffing solutions.',
  keywords: 'recruitment agency, hiring solutions, IT recruitment, executive search, staffing services, talent acquisition',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        {children}
        <Footer />
      </body>
    </html>
  )
}
